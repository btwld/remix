{
  "skill_name": "building-remix-design-system",
  "evals": [
    {
      "id": 1,
      "prompt": "We want to ship Shopify Polaris for Flutter on top of Remix so our apps can install it with remix_cli. Polaris publishes its tokens in the @shopify/polaris-tokens npm package. Set up the repository and the token plan.",
      "expected_output": "Scaffolds a public registry repository rather than a pub package: an analyzer-checked authoring package (publish_to: none) mirroring the installed layout (lib/theme, lib/components), specs/tokens.yaml, an ADR, a derivation script with --check, and registry/index.yaml offering only the vanilla preset plus registry/vanilla/registry.yaml. Treats the npm package as a tier-1 source: an extraction script pins the exact package version in one manifest, asserts inventory counts, and writes specs/tokens.yaml deterministically; the Dart theme values are tested against that file. Takes package constraints from the official vanilla catalog instead of restating versions from memory. Tells consumers to register the repository under a namespace such as @polaris and install only @polaris items.",
      "files": [],
      "expectations": [
        "Produces a registry repository with an authoring package, derivation script, index.yaml offering vanilla, and a schema-2 catalog, not a standalone pub package.",
        "Pins the exact token package version and extracts deterministically into specs/tokens.yaml with inventory assertions.",
        "Checks the Dart theme values against specs/tokens.yaml with a test and copies package constraints from the official vanilla catalog.",
        "Documents a namespace and self-contained installation for consumers."
      ]
    },
    {
      "id": 2,
      "prompt": "Our design system should just be its own preset called acme, so people run `remix init --preset acme`. Set that up in our GitHub registry.",
      "expected_output": "Explains that a third-party registry cannot introduce a preset: init always pins the official registry, and every registry a project registers must offer the project's already-selected preset, so registry add would fail. Publishes the system under the vanilla preset in its own repository and identifies it by namespace (@acme), with consumers running remix init (vanilla) then registry add @acme and add @acme/<item>. Keeps the registry self-contained with its own theme item and no @remix dependencies.",
      "files": [],
      "expectations": [
        "States that a custom preset name is unsupported for third-party registries and why.",
        "Publishes under the vanilla preset and uses the @acme namespace to identify the system.",
        "Gives the consumer flow of remix init, registry add @acme, and add @acme/<item>.",
        "Keeps the registry self-contained with its own theme and no @remix item dependencies."
      ]
    },
    {
      "id": 3,
      "prompt": "Add a Button to our Remix design-system registry. The upstream system has primary/secondary/plain variants, three sizes (28/36/44px), a 3px focus ring, and loading buttons keep their variant color with a white spinner.",
      "expected_output": "Writes specs/components/button.yaml first. Implements a top-level @MixWidget(target: RemixButton.new) recipe in the authoring package with a non-nullable enum parameter named variant (values primary, secondary, plain), a size enum mapped to 28/36/44 through a metrics switch, a caller style merged last, and token-only styling. Because RemixButton folds loading into the disabled state, declares bool loading on the recipe (shared with RemixButton's loading parameter) and keeps the variant fill in the disabled fragment when loading. Draws the focus ring with containerEffects outline in onFocusVisible so layout does not shift. Adds widget tests for measured heights, focus ring size stability, and loading fill using pump instead of pumpAndSettle, then derives the template and adds a catalog item with registryDependencies theme, a generated button.g.dart entry, and codegen constraints.",
      "files": [],
      "expectations": [
        "Writes the component worksheet before code.",
        "Uses an @MixWidget(target: RemixButton.new) recipe with a variant enum in the upstream vocabulary and merges caller style last.",
        "Handles loading as the disabled state with a shared bool loading parameter that keeps the variant fill.",
        "Uses an outline effect in onFocusVisible for the focus ring and tests with pump rather than pumpAndSettle.",
        "Derives the template and adds a catalog item with the theme dependency, generated part, and codegen constraints."
      ]
    },
    {
      "id": 4,
      "prompt": "We don't have any code for this design system — just a 40-page brand guidelines PDF and a handful of @2x PNG screenshots of the buttons and form fields. Build the token layer from these.",
      "expected_output": "Classifies the PDF as tier 3 and the screenshots as tier 4 per domain, inventories every token before filling values, and hand-authors specs/tokens.yaml where each value carries a citation (PDF page, or screenshot file and coordinates) and a confidence (specified, measured, assumed; assumed values also listed in worksheet approximations). Calibrates screenshot scale before measuring and samples flat color regions. Writes the values into theme_data.dart and tests them against specs/tokens.yaml; builds no OCR pipeline or generator and invents no scales the sources do not define.",
      "files": [],
      "expectations": [
        "Classifies the PDF as tier 3 and screenshots as tier 4 and inventories tokens before values.",
        "Records a citation and confidence for every value in specs/tokens.yaml, with assumptions echoed in worksheets.",
        "Calibrates screenshot scale and samples flat color regions.",
        "Tests theme_data values against specs/tokens.yaml without building an extraction pipeline or inventing scales."
      ]
    },
    {
      "id": 5,
      "prompt": "We have no design assets at all. Our brand is 'calm, premium, dark-first', primary color is teal #0D9488, and we use Inter. Build a Remix design system registry from that.",
      "expected_output": "Commits the brief verbatim as the pinned source, asks about or confirms missing constraints (modes, density, gray temperature) instead of silently deciding them, decides the token inventory and theme model first and records them in an ADR, then authors specs/tokens.yaml with designed or derived values produced by documented rules (for example a perceptually even teal ramp, a modular type scale, a 4px spacing base), each citing the brief or the rule. Builds the vanilla-shaped theme item with dark as a first-class mode and publishes through the standard registry repository and scratch-app loop.",
      "files": [],
      "expectations": [
        "Commits the brief and surfaces missing product constraints rather than inventing them.",
        "Defines the token inventory and theme model in an ADR before authoring values.",
        "Marks values designed or derived with citations to the brief or documented rules.",
        "Uses the standard theme item shape and registry repository workflow."
      ]
    },
    {
      "id": 6,
      "prompt": "Our design-system registry change is ready on the branch fix-select. How do we test it before our apps get it, and how should our apps follow releases?",
      "expected_output": "Pushes the branch and tests from a scratch Flutter app initialized with the vanilla preset and a prefix different from the authoring word: registry add @acme --repository <owner/repo> --path registry --ref fix-select, add @acme/select --dry-run to catch catalog and placeholder errors, then add to install, generate, and analyze, and runs the app. Iterates with registry update @acme and add --overwrite. Recommends a stable branch fast-forwarded only after the scratch app passes, explains that tags and SHAs stay fixed until consumers pass --ref, and that moving the branch never changes an app until it updates its pin. Notes that local and private registries are not supported.",
      "files": [],
      "expectations": [
        "Tests through a pushed branch in a scratch vanilla app, using a prefix different from the authoring word.",
        "Uses add --dry-run before a real add, and registry update plus add --overwrite to iterate.",
        "Recommends a stable branch advanced only after testing and explains branch versus tag or SHA pins.",
        "States that local and private registries are not supported."
      ]
    },
    {
      "id": 7,
      "prompt": "Review this template from our registry before we publish it:\n\n```dart\nimport 'package:flutter/material.dart';\nimport 'package:mix/mix.dart';\nimport 'package:remix/remix.dart';\n\n/// Implements the Acme brand guide v2.\n@MixWidget(target: RemixBadge.new)\nBoxStyler {{typePrefix}}BadgeStyle({ {{typePrefix}}BadgeTone tone = .neutral }) => BoxStyler().color(Colors.teal);\n```",
      "expected_output": "Flags that templates should be derived from an analyzer-checked authoring package rather than hand-written; that package:flutter/material.dart and Colors must go (widgets layer only, token-based color); that package:mix must be reached through package:remix/remix.dart; that mix_annotations must be imported with a part directive for the generated file and a generated entry plus codegen constraints in the catalog; that the recipe must return the Remix widget's own styler type rather than BoxStyler; that the doc comment leaks the authoring word or brand name into consumer source; and that the enum parameter should be named variant (non-nullable) if named constructors are wanted. Styling should use theme tokens, not a hard-coded color.",
      "files": [],
      "expectations": [
        "Rejects the Material import and hard-coded Colors in favor of widgets-only code and theme tokens.",
        "Requires reaching Mix through package:remix/remix.dart and adding mix_annotations with a part directive and generated catalog entry.",
        "Flags the authoring word or brand name leaking through the doc comment.",
        "Recommends deriving templates from an analyzed authoring package instead of hand-editing .tmpl files."
      ]
    },
    {
      "id": 8,
      "prompt": "Our Flutter app already ran `remix init --preset fortal` and installed card, badge, and menu. Restyle them to our brand colors and build a product dashboard; we are not publishing anything for other apps.",
      "expected_output": "Routes the task to using-remix: the application owns its installed source, so brand changes happen in its installed theme and recipes. Does not create a registry repository, authoring package, derivation script, token extraction pipeline, or a parallel component catalog.",
      "files": [],
      "expectations": [
        "Routes single-application restyling of installed source to using-remix.",
        "Does not scaffold a registry repository, authoring package, or token pipeline.",
        "Does not rename or regenerate the installed component catalog."
      ]
    }
  ]
}
