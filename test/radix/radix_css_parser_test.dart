import 'package:flutter_test/flutter_test.dart';
// Parser consolidated into scripts; keep test placeholder for now.
// Original functionality covered by extraction script helpers.

void main() {
  test('radix css parser consolidated', () {
    expect(true, true, reason: 'Parser consolidated into extraction script');
  }, skip: 'Parser moved into scripts/extract_radix_tokens.dart');
}
