name: naked_ui
description: A library of behavior-first UI components for Flutter that separate state from presentation.
repository: https://github.com/conceptadev/remix/tree/main/packages/naked_ui
issue_tracker: https://github.com/conceptadev/remix/issues
license: BSD-3-Clause
version: 1.0.0
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  meta: ^1.18.0


flutter:
  uses-material-design: true
